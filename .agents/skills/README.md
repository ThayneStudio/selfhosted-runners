# Agent Skills (Canonical)

Single source of truth for project skills. Provider dirs (`.claude/skills`, `.grok/skills`, `.codex/skills`, `.gemini/skills`, `.opencode/skills`, `.cursor/skills`) are **symlinks** back here.

Do not edit copies in provider directories — edit `.agents/skills/<name>/`.
